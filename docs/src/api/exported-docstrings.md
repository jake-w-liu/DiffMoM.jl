# Exported Docstrings

These pages render the source docstrings for every exported binding, grouped by
implementation subsystem. Start with a task-oriented API chapter when choosing
an entry point; use this index for exact signatures and defaults.

The documentation build uses `checkdocs=:exports`, so an exported binding
without a docstring fails the build.
